<%
  // Parametri:
  // title (string): Naslov bedža (opciono)
  // text (string): Dodatni opis (opciono)
  // icon (string): Material symbol ikonica (opciono)
%>

<div class="info-badge">
  <% if (icon) { %>
    <div class="info-badge__icon">
      <span class="material-symbols-outlined"><%= icon %></span>
    </div>
  <% } %>

  <% if (title) { %>
    <div class="info-badge__title"><%= title %></div>
  <% } %>

  <% if (text) { %>
    <div class="info-badge__text"><%= text %></div>
  <% } %>
</div>
